{
  "extends": "@mainset/dev-stack-fe/tsconfig--esm",
  "compilerOptions": {
    "outDir": "./dist/esm",
    // NOTE: remove when webpack config will be migrated to TS
    "allowJs": true,
    // TODO: review possibility to move into {common} config
    "target": "es2017"
  }
}
